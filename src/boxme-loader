[ Config ]

command=boxme-loader

boxme-loader=execute hash .boxme-suite; boxme-loader loading-crash;

shell.name=boxme-loader
shell.args=loading-crash,init

loading-crash=if ($OUTPUT == 0) bg execute warn BoxME installation is broken, crashing...; sleep 5; exit;

init=
